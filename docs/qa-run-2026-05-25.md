# TurnMap QA Run - 2026-05-25

Scope: `0.6.0` Topic Analysis MVP release-candidate verification after review-panel, link-suggestion status, and deep-research-style jump fixes.

## Automated Verification

| Check | Result | Notes |
| --- | --- | --- |
| Unit tests | Pass | `npm.cmd run test:unit` passed: 123 tests, 0 failures. |
| Type check | Pass | `npm.cmd run typecheck` completed with no TypeScript errors. |
| Production build | Pass | `npm.cmd run build` completed and generated `dist/`. |
| Extension package | Pass | `npm.cmd run package` rebuilt and packaged `release/turnmap-v0.6.0.zip`. |

## Focused Regression Coverage

| Area | Result | Evidence |
| --- | --- | --- |
| Topic Analysis MVP | Pass | Candidate scoring, existing-link filtering, caps, task-log support, and localized UI wiring are covered by unit tests. |
| Suggestion panel overflow | Pass | Regression test verifies the panel uses a constrained scrollable list for overflow candidates. |
| Suggest Links status feedback | Pass | Regression test verifies request, waiting, filtering, and complete phases are wired to status/task messages. |
| Accept/reject/clear feedback | Pass | Regression test verifies review actions have localized status messages. |
| Deep-research-style jump fallback | Pass | Regression test verifies ChatGPT jump lookup can use visible user-only markers while still requiring target turn-index mapping when known turns exist. |

## Manual QA Still Recommended

- Load `dist/` as an unpacked extension and run `Analyze Topics` on a long conversation.
- Confirm 5+ candidate links can be scrolled, edited, accepted, rejected, cleared, and bulk-accepted.
- In a ChatGPT conversation with deep research or folded long answers, click adjacent nodes and confirm the jump does not first drift upward before moving downward.
- Confirm repeated identical prompts either jump to the correct mapped turn or report a clear failure instead of jumping to the wrong visible duplicate.

## Residual Risk

- Very long or heavily virtualized pages can still depend on the current site DOM loading enough user markers to map a target turn.
- Provider embeddings and API refine remain out of scope for the `0.6.0` MVP and should be considered later only after this local review-first flow is stable.
