# ChatMap QA Re-run - 2026-05-12

This record refreshes the release checks after the latest conversation extraction
fixes for visible assistant text, image-only replies, and fallback nodes.

## Automated Checks

| Check | Result | Notes |
| --- | --- | --- |
| `npm.cmd run typecheck` | Pass | TypeScript completed with `tsc --noEmit`. |
| `npm.cmd run build` | Pass | Vite rebuilt side panel, full page, settings page, content script, and service worker. |
| `npm.cmd run package` | Pass | Rebuilt `dist/`, validated manifest/package structure, and generated `release/chatmap-v0.1.0.zip`. |
| `dist/manifest.json` review | Pass | Manifest v3, Edge side panel, options page, content script, permissions, host permissions, optional host permissions, and launcher icon resource remain present. |
| Sensitive-info scan | Pass | No real API keys, personal paths, temporary screenshot paths, or test endpoints found. Matches are variable names, documentation checklist text, and the expected `Authorization: Bearer ${settings.apiKey}` code template. |
| Repository hygiene review | Pass | `node_modules/`, `dist/`, `release/`, `.env`, logs, and temporary files remain ignored. |

## Package Output

- Release package: `release/chatmap-v0.1.0.zip`
- Package size: `2066863` bytes
- Generated at local time: `2026-05-12 12:24`

## Re-run Of Earlier Phases

- Phase 1 repository hygiene was rechecked after the latest extraction fixes.
- Phase 2 open-source metadata files remain present.
- Phase 3 README structure remains current, including the Chinese README.
- Phase 4 product-readiness additions remain documented.
- Phase 5 build and package checks were rerun successfully.
- Phase 6 manifest and permission assumptions still match `dist/manifest.json`.

## Release Decision

The current package is ready to continue the GitHub pre-release flow, pending any
additional manual browser checks the project owner wants before publishing.
